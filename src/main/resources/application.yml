spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/platform?serverTimezone=UTC&characterEncoding=UTF-8
    username: subscribe_admin
    password: subscribe_admin

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false